import 'package:flutter/material.dart';


class PaketData extends StatelessWidget {
  const PaketData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Text("Paket Data"),
      ),
    );
  }
}